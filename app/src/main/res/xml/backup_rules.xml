<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude database from backup (privacy) -->
    <exclude domain="database" path="notification_manager_db"/>
    <exclude domain="database" path="notification_manager_db-shm"/>
    <exclude domain="database" path="notification_manager_db-wal"/>

    <!-- Exclude sensitive preferences -->
    <exclude domain="sharedpref" path="app_prefs.xml"/>
</full-backup-content>